package createAndUseVariables

private fun main() {
    // The name of the variable is count. The data type is Int, and the initial value is 2.
    val unreadCount = 2
    val readCount = 100
    println("You have ${unreadCount + readCount} total messages in your box.")
}